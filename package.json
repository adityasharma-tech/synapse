{
  "name": "synapse",
  "module": "index.ts",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "lerna": "^8.2.2"
  },
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "dev": "concurrently --names \"server,web\" --prefix-colors \"yellow,blue\" \"bun --cwd=apps/server dev\"  \"bun --cwd=apps/web dev\""
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "rollup": "4.39.0"
  },
  "overrides": {
    "rollup": "4.39.0"
  }
}
