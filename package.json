{
    "name": "synapse",
    "version": "0.0.1",
    "packageManager": "yarn@1.22.22",
    "devDependencies": {
        "@commitlint/cli": "^19.8.0",
        "@commitlint/config-conventional": "^19.8.0",
        "concurrently": "^9.1.2",
        "husky": "^9.1.7",
        "lerna": "^8.2.2",
        "prettier": "^3.5.3",
        "rimraf": "^6.0.1"
    },
    "private": true,
    "scripts": {
        "build:packages": "lerna run build",
        "build:apps": "yarn --cwd=apps/server build; yarn --cwd=apps/web build",
        "dev": "concurrently --names \"server,web\" --prefix-colors \"yellow,blue\" \"yarn --cwd=apps/server dev\"  \"yarn --cwd=apps/web dev\"",
        "start": "concurrently --names \"server,web\" --prefix-colors \"yellow,blue\" \"yarn --cwd=apps/server start\"  \"yarn --cwd=apps/web dev\"",
        "prepare": "husky",
        "format": "prettier . --write",
        "clean": "rimraf ./packages/**/dist ./website/**/dist"
    },
    "type": "module",
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "dependencies": {
        "drizzle-orm": "^0.42.0",
        "pg": "^8.15.5",
        "rollup": "4.39.0"
    },
    "overrides": {
        "rollup": "4.39.0"
    }
}
